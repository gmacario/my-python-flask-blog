pipeline {
  agent {
    dockerfile {
      filename 'Dockerfile'
    }

  }
  stages {
    stage('Build') {
      steps {
        echo 'TODO: Build'
        sh '''#/bin/bash
docker --version
# docker-compose --version
# docker-compose build --pull
# EOF'''
      }
    }
  }
}